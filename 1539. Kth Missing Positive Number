class Solution {
    public int findKthPositive(int[] a, int k) {
        int l = 0, r = a.length;
        while (l < r) {
            int m = (l + r) / 2;
            if (a[m] - m - 1 < k) l = m + 1;
            else r = m;
        }
        return l + k;
    }
}

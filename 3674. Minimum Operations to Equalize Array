class Solution {
    public int minOperations(int[] nums) {
        int target = nums[0];

        for(int num : nums){
            if(num != target)
                return 1;
        }
        return 0 ;
        
        
    }
}

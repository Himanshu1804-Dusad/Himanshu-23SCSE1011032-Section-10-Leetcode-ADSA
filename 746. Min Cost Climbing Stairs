class Solution {
    public int minCostClimbingStairs(int[] cost) {
        int a = 0, b= 0;

        for(int i = 0; i < cost.length;i++){
            int curr = cost[i] + Math.min(a,b);

            a = b;
            b = curr;
        }

        return Math.min(a,b);
        
    }
}
